package com.dwp.services.platform.workplace;

/** Canonical native target kinds shared by administrator application services and route guards. */
public enum WorkplaceDelegatedAdminTargetType {
    SITE, FLOOR, RESOURCE, ZONE, SECTION, ACCESS_RULE, POLICY_OVERRIDE,
    FLOOR_PLAN_REVISION, BOOKING, CLOSURE, FACILITY_REQUEST
}
